package com.sighs.sophisticatedsorter.utils;

/** Updates Core's private search-box expansion anchor after a group drag. */
public interface SearchBoxPositionAccess {
    void sophisticatedSorter$setMaximizedPosition(int x, int width);
}
